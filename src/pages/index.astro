---
import Layout from '../layouts/Layout.astro';
import PlayListItemCard from '@/components/PlayListItemCard.astro'

import { playlists } from '@/lib/data';

const sortedPlayLists = playlists.sort((a,b) => a.title.localeCompare(b.title))
---

<Layout title="Spotify clone">
    <div 
    id="playlist-container"
    class="relative transition-all duration-1000 bg-red-600">
        <!-- <PageHeader -->
        <div class="relative z-10 px-6 pt-10">
            <!-- Greetings -->
            <h1 class="text-4xl font-bold">Bienvenido</h1>
            <div class="flex flex-wrap mt-6 gap-4">
                { sortedPlayLists.map(playlist => <PlayListItemCard playlist={playlist}/>) }
            </div>
            <div
                class="absolute inset-0 bg-gradient-to-t from-zinc-900 via-zinc-900/80 -z-[1]">
            </div>
        </div>
    </div>
</Layout>

<style></style>
